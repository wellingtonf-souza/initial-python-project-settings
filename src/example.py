def add_one(number: int) -> int:
    """Método que realiza uma soma simples com 1 ao número fornecido.

    Args:
        number: Número a ser adicionado mais um

    Returns:
        Resultado da soma
    """
    return number + 1
